apiVersion: v1
kind: Service
metadata:
  name: admin-dashboard
  namespace: admin-dashboard
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: admin-dashboard
