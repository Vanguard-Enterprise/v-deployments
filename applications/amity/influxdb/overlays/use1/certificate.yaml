apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: influxdb-tls
  namespace: amity-influx
spec:
  secretName: influxdb-tls-secret
  dnsNames:
    - influx-use1.amityrust.com
  issuerRef:
    name: letsencrypt-production-amity
    kind: ClusterIssuer
