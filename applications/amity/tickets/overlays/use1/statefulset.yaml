apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: ticket-bot
  namespace: amity-tickets
spec:
  selector:
    matchLabels:
      app: ticket-bot
  serviceName: ticket-bot
  replicas: 1
  podManagementPolicy: OrderedReady
  updateStrategy:
    type: RollingUpdate
    rollingUpdate:
      partition: 0
  template:
    metadata:
      labels:
        app: ticket-bot
    spec:
      terminationGracePeriodSeconds: 15
      imagePullSecrets:
        - name: harbor-pull-secret
      containers:
        - name: bot
          image: registry.vngenterprise.com/k8s/ticket-bot:9fe6da110725caad45ff8c6e563dd40d6de52e70
          lifecycle:
            preStop:
              exec:
                command: ["sh", "-c", "kill -s SIGTERM 1 && sleep 5"]
          envFrom:
            - secretRef:
                name: amity-tickets
            - configMapRef:
                name: ticket-config
