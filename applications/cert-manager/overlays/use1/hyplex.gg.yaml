apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: hyplex
  namespace: traefik
spec:
  secretName: hyplex-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  commonName: "*.hyplex.gg"
  dnsNames:
    - "*.hyplex.gg"
    - hyplex.gg