apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mancini
  namespace: traefik
spec:
  secretName: mancini-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  commonName: "*.mancini.beer"
  dnsNames:
    - "*.mancini.beer"
    - mancini.beer