apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: rustlens
  namespace: gloo-system
spec:
  secretName: rustlens-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  commonName: "*.rustlens.com"
  dnsNames:
    - "*.rustlens.com"
    - rustlens.com