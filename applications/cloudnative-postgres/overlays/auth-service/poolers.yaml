apiVersion: postgresql.cnpg.io/v1
kind: Pooler
metadata:
  name: auth-service-pooler-rw
  namespace: auth-service-db
spec:
  cluster:
    name: auth-service
  instances: 2
  type: rw
  pgbouncer:
    poolMode: transaction
    parameters:
      max_client_conn: "1000"
      default_pool_size: "10"
---
apiVersion: postgresql.cnpg.io/v1
kind: Pooler
metadata:
  name: auth-service-pooler-ro
  namespace: auth-service-db
spec:
  cluster:
    name: auth-service
  instances: 2
  type: ro
  pgbouncer:
    poolMode: transaction
    parameters:
      max_client_conn: "1000"
      default_pool_size: "10"
