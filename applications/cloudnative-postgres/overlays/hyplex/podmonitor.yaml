apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: hyplex-pg-cnpg
  namespace: hyplex-pg
  labels:
    instance: primary
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: hyplex
  podMetricsEndpoints:
    - port: metrics
      interval: 15s
