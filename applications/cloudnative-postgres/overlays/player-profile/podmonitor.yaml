apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: player-profile-db-cnpg
  namespace: player-profile-db
  labels:
    instance: primary
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: player-profile
  podMetricsEndpoints:
    - port: metrics
      interval: 15s
