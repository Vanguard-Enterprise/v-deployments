apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: rustlens-db-cnpg
  namespace: rustlens-db
  labels:
    instance: primary
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: rustlens
  podMetricsEndpoints:
    - port: metrics
      interval: 15s