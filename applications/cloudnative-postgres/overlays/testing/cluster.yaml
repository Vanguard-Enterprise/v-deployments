apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: testing
  namespace: testing
spec:
  instances: 3

  storage:
    size: 1Gi
  resources:
    requests:
      memory: "32Mi"
      cpu: "50m"
    limits:
      memory: "128Mi"
      cpu: "100m"