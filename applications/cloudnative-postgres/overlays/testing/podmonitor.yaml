apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: testing-cnpg
  namespace: testing
  labels:
    instance: primary
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: testing
  podMetricsEndpoints:
    - port: metrics
      interval: 15s