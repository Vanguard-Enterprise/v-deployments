apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: world-service-db
  namespace: world-service-db
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: world-service
  podMetricsEndpoints:
    - port: metrics
