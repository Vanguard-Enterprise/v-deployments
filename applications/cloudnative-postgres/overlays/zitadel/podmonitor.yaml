apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: zitadel-db-cnpg
  namespace: zitadel-db
  labels:
    instance: primary
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: zitadel
  podMetricsEndpoints:
    - port: metrics
      interval: 15s
