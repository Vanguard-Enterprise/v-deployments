apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: grafana-db-cnpg
  namespace: grafana
  labels:
    instance: primary
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: grafana-db
  podMetricsEndpoints:
    - port: metrics
      interval: 60s
