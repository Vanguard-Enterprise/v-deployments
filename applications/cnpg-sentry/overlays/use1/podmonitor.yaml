apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: sentry-db-cnpg
  namespace: sentry
  labels:
    instance: primary
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: sentry-db
  podMetricsEndpoints:
    - port: metrics
      interval: 60s
