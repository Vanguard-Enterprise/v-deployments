apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: allow-secrets-configmaps
  namespace: cert-manager
rules:
- apiGroups: [""]
  resources: ["secrets", "configmaps"]
  verbs: ["list", "get", "watch"]