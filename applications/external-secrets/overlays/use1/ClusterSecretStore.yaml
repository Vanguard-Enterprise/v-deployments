apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: vault-backend
spec:
  provider:
    vault:
      server: "https://10.1.45.150"
      path:   "kv"
      version: "v2"
      auth:
        kubernetes:
          mountPath: "use1"
          role:      "external-secrets"