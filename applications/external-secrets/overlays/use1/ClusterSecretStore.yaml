apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: vault-backend
spec:
  provider:
    vault:
      server: "http://vault.vng.bet:8200"
      path:   "kv"
      version: "v2"
      auth:
        kubernetes:
          mountPath: "kubernetes-use1"
          role:      "ext-secrets-role-use1"