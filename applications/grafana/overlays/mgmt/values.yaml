datasources:
  datasources.yaml:
    apiVersion: v1
    datasources:
    - name: loki
      type: loki
      url: http://loki-gateway.loki
      access: proxy
      isDefault: true
      editable: true

persistence:
  storageClassName: longhorn
  enabled: true
  size: 2Gi
