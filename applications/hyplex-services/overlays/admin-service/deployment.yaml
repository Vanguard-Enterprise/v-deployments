apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-service
  namespace: hyplex-admin
spec:
  replicas: 2
  selector:
    matchLabels:
      app: admin-service
  template:
    metadata:
      labels:
        app: admin-service
    spec:
      imagePullSecrets:
        - name: github-pull-secret
      containers:
        - name: admin-service
          image: ghcr.io/hyplex-network/admin-service:latest
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          envFrom:
            - configMapRef:
                name: hyplex-services-config
            - secretRef:
                name: admin-service-secrets
          env:
            - name: PORT
              value: "8080"
            - name: SEED_ADMIN_USERNAME
              value: "frank"
            - name: SEED_ADMIN_EMAIL
              value: "frankdobrovolny@outlook.com"
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: admin-service
                topologyKey: kubernetes.io/hostname
