apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: admin-service-secrets
  namespace: hyplex-admin
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: admin-service-secrets
    creationPolicy: Owner
  data:
    - secretKey: REDIS_PASSWORD
      remoteRef:
        key: "hyplex-redis/production"
        property: password
    - secretKey: JWT_SECRET
      remoteRef:
        key: "hyplex-admin/production"
        property: jwt_secret
