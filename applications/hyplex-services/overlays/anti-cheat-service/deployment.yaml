apiVersion: apps/v1
kind: Deployment
metadata:
  name: anti-cheat-service
  namespace: hyplex-anticheat
spec:
  replicas: 2
  selector:
    matchLabels:
      app: anti-cheat-service
  template:
    metadata:
      labels:
        app: anti-cheat-service
    spec:
      imagePullSecrets:
        - name: github-pull-secret
      containers:
        - name: anti-cheat-service
          image: ghcr.io/hyplex-network/anti-cheat-service:latest
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          envFrom:
            - configMapRef:
                name: hyplex-services-config
            - secretRef:
                name: anti-cheat-service-secrets
          env:
            - name: PORT
              value: "8080"
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: anti-cheat-service
                topologyKey: kubernetes.io/hostname
