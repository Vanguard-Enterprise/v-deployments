apiVersion: v1
kind: Service
metadata:
  name: configuration-service-lb
  namespace: hyplex-configuration
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: configuration-service
