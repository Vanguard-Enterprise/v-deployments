apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: economy-service-secrets
  namespace: hyplex-economy
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: economy-service-secrets
    creationPolicy: Owner
  data:
    - secretKey: REDIS_PASSWORD
      remoteRef:
        key: "hyplex/production/redis"
        property: password
