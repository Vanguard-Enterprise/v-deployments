apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: friends-service-secrets
  namespace: hyplex-friends
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: friends-service-secrets
    creationPolicy: Owner
  data:
    - secretKey: REDIS_PASSWORD
      remoteRef:
        key: "hyplex/production/redis"
        property: password
