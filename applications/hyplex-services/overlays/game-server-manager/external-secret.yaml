apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: game-server-manager-secrets
  namespace: hyplex-gsm
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: game-server-manager-secrets
    creationPolicy: Owner
  data:
    - secretKey: REDIS_PASSWORD
      remoteRef:
        key: "hyplex/production/redis"
        property: password
