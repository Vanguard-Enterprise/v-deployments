apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: lobby-service-secrets
  namespace: hyplex-lobby
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: lobby-service-secrets
    creationPolicy: Owner
  data:
    - secretKey: REDIS_PASSWORD
      remoteRef:
        key: "hyplex-redis/production"
        property: password
