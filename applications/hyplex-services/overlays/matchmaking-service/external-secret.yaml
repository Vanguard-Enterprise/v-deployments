apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: matchmaking-service-secrets
  namespace: hyplex-matchmaking
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: matchmaking-service-secrets
    creationPolicy: Owner
  data:
    - secretKey: REDIS_PASSWORD
      remoteRef:
        key: "hyplex-redis/production"
        property: password
