apiVersion: apps/v1
kind: Deployment
metadata:
  name: player-profile-service
  namespace: hyplex-player-profile
spec:
  replicas: 2
  selector:
    matchLabels:
      app: player-profile-service
  template:
    metadata:
      labels:
        app: player-profile-service
    spec:
      imagePullSecrets:
        - name: github-pull-secret
      containers:
        - name: player-profile-service
          image: ghcr.io/hyplex-network/player-profile-service:latest
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          envFrom:
            - configMapRef:
                name: hyplex-services-config
            - secretRef:
                name: player-profile-service-secrets
          env:
            - name: PORT
              value: "8080"
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 5
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "200m"
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: player-profile-service
                topologyKey: kubernetes.io/hostname
