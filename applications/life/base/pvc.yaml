apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: life-data
  namespace: life
  labels:
    app: life
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
