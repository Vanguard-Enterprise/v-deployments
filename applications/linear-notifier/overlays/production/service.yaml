apiVersion: v1
kind: Service
metadata:
  name: linear-notifier
  namespace: linear-notifier
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
  selector:
    app: linear-notifier
