apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: loki-s3
  namespace: loki
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: loki-s3
    creationPolicy: Owner
  data:
  - secretKey: accessKeyId
    remoteRef:
      key: "grafana/loki/loki-s3"
      property: accessKeyId
  - secretKey: secretAccessKey
    remoteRef:
      key: "grafana/loki/loki-s3"
      property: secretAccessKey
