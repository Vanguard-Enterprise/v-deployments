apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: mimir
  namespace: mimir
  labels:
    instance: primary
spec:
  selector:
    matchLabels:
      app: mimir
  endpoints:
    - port: http
      path: /metrics
      interval: 60s
