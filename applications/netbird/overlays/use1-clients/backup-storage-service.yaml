apiVersion: v1
kind: Service
metadata:
  name: backup-storage
  namespace: netbird-client
spec:
  type: ClusterIP
  selector:
    app: netbird-client
  ports:
    - name: s3
      port: 443
      targetPort: s3
      protocol: TCP
