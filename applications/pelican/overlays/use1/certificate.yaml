apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pelican-tls
  namespace: pelican
spec:
  secretName: pelican-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - gdev.vngenterprise.com
