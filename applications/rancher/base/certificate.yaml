apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: rancher-vng
  namespace: cattle-system
spec:
  commonName: rancher.vng.bet
  dnsNames:
    - rancher.vng.bet
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  secretName: tls-rancher-ingress