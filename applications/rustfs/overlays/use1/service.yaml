apiVersion: v1
kind: Service
metadata:
  name: rustfs
  namespace: hyplex-rustfs
spec:
  type: ClusterIP
  ports:
    - name: s3
      port: 9000
      targetPort: s3
      protocol: TCP
    - name: console
      port: 9001
      targetPort: console
      protocol: TCP
  selector:
    app: rustfs
