apiVersion: v1
kind: Service
metadata:
  name: rustlens-frontend
  namespace: rustlens-frontend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
  selector:
    app: rustlens-frontend
