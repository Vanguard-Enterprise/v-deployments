---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: devtron-ingress
  namespace: devtroncd
  annotations: 
    kubernetes.io/ingress.class: traefik-external
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`infra.vng.bet`) # change to your domain
      kind: Rule
      services:
        - name: devtron-service
          port: 80
      #middlewares:
        #- name: default-headers
  tls:
    store:
      name: vng-store
      namespace: traefik