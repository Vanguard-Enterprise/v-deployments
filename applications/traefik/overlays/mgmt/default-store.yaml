apiVersion: traefik.io/v1alpha1
kind: TLSStore
metadata:
  name: default
  namespace: traefik
spec:
  defaultCertificate:
    secretName: "vng-tls"
---
apiVersion: traefik.io/v1alpha1
kind: TLSStore
metadata:
  name: amity
  namespace: traefik
spec:
  defaultCertificate:
    secretName: "amity-tls"
