apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: kube-apiserver
  namespace: kube-system
spec:
  entryPoints:
    - kube-apiserver
  routes:
    - match: HostSNI(`api.cluster1.vng.bet`)
      services:
        - name: kubernetes
          namespace: default
          port: 443
  tls:
    passthrough: true