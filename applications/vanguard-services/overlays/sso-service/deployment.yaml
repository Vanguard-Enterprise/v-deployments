apiVersion: apps/v1
kind: Deployment
metadata:
  name: sso-service
  namespace: vanguard-sso
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sso-service
  template:
    metadata:
      labels:
        app: sso-service
    spec:
      imagePullSecrets:
        - name: github-pull-secret
      containers:
        - name: sso-service
          image: ghcr.io/vanguard-enterprise/sso-service:latest
          ports:
            - name: http
              containerPort: 8082
              protocol: TCP
            - name: grpc
              containerPort: 9092
              protocol: TCP
          envFrom:
            - configMapRef:
                name: vanguard-services-config
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: sso-service-secrets
                  key: DATABASE_URL
            - name: ENCRYPTION_KEY
              valueFrom:
                secretKeyRef:
                  name: sso-service-secrets
                  key: ENCRYPTION_KEY
          volumeMounts:
            - name: jwt-key
              mountPath: /etc/secrets/jwt
              readOnly: true
          livenessProbe:
            httpGet:
              path: /v1/healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /v1/healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: jwt-key
          secret:
            secretName: sso-service-secrets
            items:
              - key: JWT_PUBLIC_KEY
                path: public.pem
