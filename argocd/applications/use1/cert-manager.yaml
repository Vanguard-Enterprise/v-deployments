apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cert-manager-use1
  namespace: argocd
spec:
  project: security
  source:
    repoURL: https://github.com/your-org/repo-root.git
    path: applications/cert-manager/overlays/use1
    targetRevision: main
  destination:
    server: https://kubernetes.default.svc
    namespace: cert-manager
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
