apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: lobby-service
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Vanguard-Enterprise/v-deployments
    path: applications/hyplex-services/overlays/lobby-service
    targetRevision: main
  destination:
    server: https://kubernetes.default.svc
    namespace: hyplex-lobby
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
