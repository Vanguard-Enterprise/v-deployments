apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: metallb-use1
  namespace: devtroncd
spec:
  project: default
  source:
    repoURL: https://github.com/Vanguard-Enterprise/v-deployments
    path: applications/metallb/overlays/use1
    targetRevision: main
  destination:
    name: use1
    namespace: metallb-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
